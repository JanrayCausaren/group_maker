import 'package:flutter/material.dart';

class DirectoryDetailsScreen extends StatelessWidget {
const DirectoryDetailsScreen({ super.key });


  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: const Text('Details'),
      ),
      body: Container(),
    );
  }
}